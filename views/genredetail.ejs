<h2>
  <%= genre.name %>
</h2>
<h4>Books in Genre</h4>
<dl>
  <% booksInGenre.forEach(book=> { %>
    <dt><a href="#" hx-get="<%= book.url %>">
        <%= book.title %>
      </a> </dt>
    <dd>
      <%= book.summary %>
    </dd>
    <% }) %>
</dl>
